<div class="topnav">
  <a class="active" href="/">Home</a>
  <div class="login-container">
      <% if(loggedInUser) {  %> 
        <li class="nav-item">
            <span class="navbar-text mr-2 text-success"><strong><%= loggedInUser %></strong></span>
          </li>
          <li class="nav-item">
            <form action="/log_out" method="POST" class="form-inline">
              <button class="btn btn-outline-dark">Log Out</button>
            </form>
          </li>
    <% } else {%> 
      <a class="nav-item nav-link" href="/sign_in">Sing In</a>
    <% }%>
  </div>
</div>
